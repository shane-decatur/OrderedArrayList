# OrderedArrayList
We mostly talked about the concept of generics and how they could be used. We all agreed that generics were classes that could accept different types of objects, like with Class<T>, where T can be different objects. Someone compared it to open immigration and liberalization where any object could be accepted.
